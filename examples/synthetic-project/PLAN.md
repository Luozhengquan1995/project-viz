# Synthetic work plan

Build an inventory CSV importer with explicit field validation.

1. Document the expected columns and identifiers.
2. Validate required fields and detect duplicate identifiers.
3. Show an import summary after acceptance succeeds.

This plan is synthetic. It does not prescribe stages for other projects.
